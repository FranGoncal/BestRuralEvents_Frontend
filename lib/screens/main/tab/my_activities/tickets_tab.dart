import 'package:flutter/material.dart';

class TicketsTab extends StatelessWidget {
  const TicketsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Tickets tab coming next',
        style: TextStyle(fontSize: 16),
      ),
    );
  }
}